# v0.1 车身技能选项与 exact 优化

## 普通用户选项

v0.1 的四个车身槽位选择 `BodySkillOption`，英雄只作为效果来源和资料追溯。普通 UI 固定显示九类技能；“不选择”由 UI 作为空值提供。

| 技能选项 | 正式语义 | 来源英雄 |
|---|---|---|
| 全军攻击 +25% | `skill.attack +0.25`，常驻 | 书允、赫罗尼莫、马格努斯、维薇卡、鲁弗斯 |
| 全军防御 +25% | 当前只计算我方对熊输出，贡献为 0 | 来源资料尚未提供 |
| 敌军防御 -25% | `skill.defenseReduction +0.25` | 亨德里克 |
| 全军伤害 +20% | `skill.baseDamageIncrease +0.20` | 索尼娅、多米尼克、艾诗琳 |
| 易伤 +25% | `skill.vulnerable +0.25`，下一回合生效 | 格温 |
| 40%概率全军穿透 +50% | 每回合判定，当回合生效一回合 | 阿隆索、琳恩 |
| 50%概率易伤 +50% | 每回合三次普通攻击独立判定，下一回合生效且不叠幅度 | 米娅 |
| 20%概率增伤 +40%（持续3回合） | 每回合判定，立即生效；重复触发刷新三回合，不叠幅度 | 格雷格 |
| 普攻伤害 +30% | `skill.normalAttackDamageIncrease +0.30`，只放大普通部分 | 玲奈 |

原始英雄资料仍保留在英雄数据库中；不属于以上九类的旧车身效果不进入普通 UI，也不进入 v0.1 自动车身优化。

## 手动与自动约束

- 手动四槽允许 `A+A+A+A`，伤害引擎没有最大两份限制。
- 自动优化按无序 `BodySkillOption` 多重组合搜索，同一种效果最多两份。
- 九种技能恰选四个时共有 414 个合法组合；不会因多个英雄拥有相同效果而扩大空间。
- 每个组合先生成 `AggregatedBodyEffect` 和不含来源英雄身份的 `BodyEffectSignature`。伤害语义相同的签名只执行一次正式评分。

## 比例目标的可分离性审计

当前正式管线中，总容量在单次比例优化内固定；概率、持续时间、回合表、普通攻击次数及额外伤害规则均不读取兵种数量。基础伤害对每个兵种唯一的可变项是 `sqrt(n_i)`，因此十回合期望目标可严格写成：

`D = Ks*sqrt(ns) + Kl*sqrt(nl) + Km*sqrt(nm)`。

当前未发现会根据兵种数量改变概率、时序、攻击次数，或产生跨兵种数量耦合的正式技能。优化器通过一次三兵种均为正数的正式十回合计算读取各兵种期望贡献，再除以 `sqrt(n_i)` 得到 `Ks/Kl/Km`；最终候选仍交给正式伤害管线复核并生成明细。

连续松弛的最优比例为：

`ns:nl:nm = Ks^2:Kl^2:Km^2`。

连续解只用于解释，不能直接当作最终答案。

## 0.01% 网格 exact 算法

- `100.00%` 表示为 10,000 个整数 tick，三兵种 tick 和严格等于 10,000。
- 搜索树完整覆盖所有合法 tick；理论完整空间为 50,015,001 个点。
- 每个搜索节点把 largest-remainder 可产生的整数兵数范围放宽为盒约束下的连续凹优化，使用 KKT 活动集计算严格伤害上界。
- 只有节点上界已经严格低于当前第 K 名时才剪枝。因此剪枝只排除不可能进入 Top K 的区域，不是近似搜索。
- 叶节点继续调用项目既有 largest remainder；相同整数兵数通过 `TroopCountSignature` 只做一次快速 `ΣKi√ni` 评分。
- Top K 确定后才运行完整十回合模拟生成技能、概率和回合明细。

1% 和 0.1% 均与 naive 全网格 Top 1 对照；受限 0.01% 网格也逐项对照。测试必须保持比例、整数兵数和伤害一致。

## 联合优化

联合优化不再计算“全部 0.01% 比例 × 全部车身组合”的完整笛卡尔积。流程为：

1. 生成合法 `BodySkillOption` 组合并预聚合 `BodyEffect`；
2. 每个 `BodyEffect` 通过正式管线得到 `Ks/Kl/Km`；
3. 为该效果直接求 0.01% 网格 exact Top K；
4. 合并各效果候选并对最终 Top K 运行完整伤害明细。

算法没有使用随机搜索、遗传算法、退火或爬山，也没有修改基础伤害公式、largest remainder 或 Battle Report / Buff / Skill / Expert 乘区。
